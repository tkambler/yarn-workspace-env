import { Link } from 'react-router-dom';

export const SettingsFeature = () => {
  return (
    <div>
      This is the settings feature.
      <p>
        <Link to="/">Go to the Homepage</Link>
      </p>
    </div>
  );
};
